Pre-requisites:
1. Python
2. virtual environment enabler
3. pip

Follow these steps for set up:
1. Clone the repository : git clone https://github.com/SravanTurbo/feature_request_app.git
2. Go to the project directory : cd feature_request_app
3. Create a virtual environment : python3 -m venv env
4. Actiavate virtual environmet : source ./env/bin/activate
5. Install all dependencies for backend : pip install -r ./requirements.txt
6. Install all dependencies for frontend : npm install
7. Start Backend : python backend/manage.py runserver
8. Start Frontend : npm start

Now our Feature Request app is up and running

Note: Add Clients and Product Area before you start adding Feature Requests
